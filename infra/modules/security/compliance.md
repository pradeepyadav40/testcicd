# Security Module Compliance

- Provides a dedicated NSG for the Windows VM subnet.
- Supports optional RDP access with configurable source IP ranges.
- Ready for extension with additional security rules and Azure Policy.
